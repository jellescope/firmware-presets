Create a new index.json:  node indexer/indexer.js // this will also inform you if you made mistakes within the preset files

create a new branch git checkout -b <name>
after changes rerun indexer

now add changes with: git add -A
now commit the changes with: git commit -m "description"